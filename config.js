// =============================================================
// RMA Blaster — Configuration
// =============================================================
// Fill in your Supabase project URL and anon key, OR enter them
// in the app's Settings panel (they're stored in the browser).
//
// UNLIKE the old Google client secret, the anon key is SAFE to
// commit publicly: it only identifies the project. All data
// access still requires a team member to sign in, enforced
// server-side by Row Level Security.
//
// Find both values in: Supabase Dashboard → Project Settings →
// API → "Project URL" and "anon public" key.
// =============================================================

const CONFIG = {
  supabaseUrl: 'YOUR_SUPABASE_PROJECT_URL',   // e.g. https://abcdefgh.supabase.co
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY'
};
